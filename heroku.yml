setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
release:
  command:
    - python manage.py seed-db
  image: web
run:
  web: gunicorn --bind 0.0.0.0:5000 - manage:app
